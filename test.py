print("mkdir reponame, notepad filename and code and save in notepad, git init, git add . git config --global user.name <usrname>, git config --global user.email <usr email>, git branch, git commit -m "first commit", {if already repo created in browser we need to add from terminal changed then use this command} git remote add origin https://github.com/vijayaonline9991-maker/myproject.git, { if path is wrong then remove old origin and add new origin} >git remote remove origin, git push -u origin master, git commit -m "updated hi git", git push, git status")